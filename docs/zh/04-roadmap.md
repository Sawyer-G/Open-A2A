# 开发路线图

## MVP 落地步骤

### 第一阶段：Hello Open-A2A

**目标**：在两台电脑（或两个进程）间跑通「广播-响应」。

**任务**：

1. 配置本地 NATS 服务器
2. 编写 `Consumer.py`：发送意图消息
3. 编写 `Merchant.py`：监听到消息后自动回复一个 Offer

---

### 第二阶段：隐私与身份认证

**目标**：确保只有真实的 Agent 能对话。

**任务**：

1. 集成 `did:key` 加密，消息发送前必须用私钥签名
2. 集成 Solid Pod，Agent 从 Pod 中读取用户偏好，而非硬编码在脚本里

---

### 第三阶段：复杂场景模拟

**目标**：实现「买面条」的全链路闭环（A-B-C 三方）。

**任务**：

1. 加入 `Carrier.py`（配送员）
2. 实现基于合约逻辑的模拟支付流
3. 开源代码库并发布首份 RFC 文档
