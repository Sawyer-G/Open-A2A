# Open-A2A Federation Subject Bridge (NATS <-> NATS)
FROM python:3.12-slim

WORKDIR /app

COPY pyproject.toml README.md ./
COPY open_a2a/ open_a2a/
COPY federation/ federation/

RUN pip install --no-cache-dir .

CMD ["python", "federation/subject_bridge.py"]

